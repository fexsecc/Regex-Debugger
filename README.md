# Regex-Debugger
## Building
### Windows
```powershell
Set-ExecutionPolicy Bypass -Scope Process # enable running the setup ps1 script
.\setup.ps1
cmake --build build
```
### Linux/Mac
```bash
./setup.sh
cmake --build build
```
